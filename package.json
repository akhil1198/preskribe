{
	"name": "preskribe",
	"version": "1.0.0",
	"description": "Prescription managment application",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"client": "cd client && npm start",
		"server": "cd server && npm run server",
		"dev": "concurrently \"npm run client\" \"npm run server\""
	},
	"author": "Akhil S",
	"license": "ISC",
	"dependencies": {
		"concurrently": "^7.0.0"
	}
}
